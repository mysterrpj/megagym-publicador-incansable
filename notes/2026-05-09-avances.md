# Avances 2026-05-09

## Objetivo del dia
Habilitar y validar publicaciones programadas con video, manteniendo el flujo actual de imagenes para Facebook, Instagram y WhatsApp.

## Lo que hice
- Programe la publicacion del `2026-05-10 08:00` como `lista` con `posts_programados/2026-05-10_0800.jpg`.
- Agregue soporte en `publisher.py` para detectar assets por extension: imagen o video.
- El publicador ahora envia `asset_type`, `asset_url` y, para videos, `video_url` al webhook de Make.
- Agregue inputs manuales al workflow `publicar.yml`: `publicacion_fecha` y `publicacion_hora`.
- Programe y probe el video `posts_programados/2026-05-10_2000.mp4`.
- Se ajusto el escenario de Make para videos:
  - Imagen: rutas existentes para Instagram photo post y Facebook post with photos.
  - Video: rutas nuevas para Facebook Upload a Video e Instagram Create a reel post.
- Se redetermino la estructura del webhook en Make para reconocer `asset_type`, `asset_url` y `video_url`.
- Se corrigieron filtros de Make:
  - Imagen: `asset_type = image`.
  - Video: `asset_type = video`.

## Problemas encontrados
- Make recibio el webhook de video, pero fallo inicialmente porque las rutas existentes esperaban imagen.
- El escenario de Make quedo inactivo despues del error y acumulo registros en cola.
- Make no mostraba `video_url` porque el webhook tenia una estructura vieja.
- Los filtros de video quedaron inicialmente comparando contra `video_url` en lugar del texto `video`.
- La prueba local con import completo de `publisher.py` no termino dentro del timeout.
- Git local mostro warnings de permiso al leer `C:\Users\Lenovo\.config\git\ignore`.

## Soluciones aplicadas
- Se agrego soporte de `asset_type` y `video_url` en el payload hacia Make.
- Se agrego validacion generica de assets publicos para imagenes y videos.
- Se evito fallback de imagen cuando una publicacion esta programada como video.
- Se configuro Make con rutas separadas para imagen y video.
- Se activo nuevamente el escenario de Make y se redetermino su estructura.
- Se corrigieron filtros a `asset_type = video`.
- Se ejecuto una prueba final en Make con resultado `Success`; los modulos de video completaron correctamente.

## Archivos tocados
- `.github/workflows/publicar.yml`
- `MANUAL.md`
- `calendario_publicaciones.csv`
- `historial_fotos.json`
- `publisher.py`
- `posts_programados/2026-05-10_0800.jpg`
- `posts_programados/2026-05-10_2000.mp4`
- `notes/2026-05-09-avances.md`
- `TODO.md`
- `HANDOFF.md`
- `PROJECT_CONTEXT.md`
- `DECISIONS.md`

## Estado al cerrar
- Soporte de videos implementado y subido a `master`.
- Make publica videos en Facebook e Instagram/Reel cuando recibe `asset_type = video`.
- Las imagenes siguen usando el flujo actual con `asset_type = image`.
- La fila `2026-05-10 20:00` quedo como `publicada` para evitar repetir el video de prueba.

## Siguiente paso para retomar manana
Programar una nueva publicacion real de video con un archivo nuevo en `posts_programados/`, marcarla como `lista` y verificar una ejecucion automatica completa sin usar URLs fijas de prueba.
